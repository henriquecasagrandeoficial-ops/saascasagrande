# Portal unificado — Login/Hub + painéis no mesmo domínio

AUTH_SECRET="troque-por-um-segredo-aleatorio"
ADMIN_EMAIL="admin@casagrande.com"
ADMIN_PASSWORD="admin123"

# Opcional na Vercel (usa VERCEL_URL automaticamente)
# NEXT_PUBLIC_PORTAL_URL="https://seu-dominio.vercel.app"

# Banco Sistema de Confeitaria (dona-lu)
DATABASE_URL="postgresql://user:password@host/dona-lu?sslmode=require"
DIRECT_URL="postgresql://user:password@host/dona-lu?sslmode=require"

# Banco Sistema de Joias (allativa)
POSTGRES_PRISMA_URL="postgresql://user:password@host/allativa?sslmode=require"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host/allativa?sslmode=require"

# Upload de imagens (Vercel Blob) — compartilhado ou por sistema
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxxxxxx"
